module Tritium.Menu (
  mainMenu,
  menuImages
) where

import           Graphics.UI.Fungen

import           Tritium.Import

menuImages :: FilePictureList
menuImages = [("button", Nothing)]

mainMenu :: TritiumAction ()
mainMenu = undefined

